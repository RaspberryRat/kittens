<h2>All your kittens</h2>

<ul>
  <% @kittens.each do |kitten| %>
    <li><%= link_to kitten.name, kitten %></li>
    <% end %>
</ul>

<h3>Add your kitten</h3>
<%= link_to 'Add Kitten', new_kitten_path %>
